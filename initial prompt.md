### 1st prompt

I want to create a latest react-ts project. The project idea is the user can visit a location and drop a message in a particular location. the msg data is sent to backed along with logitude, lattitude and altitue. the message a user dropped can be public or specially for some people. if it is private message, only allowed members can view it. To view the message, another user have to visit the exact location and open the web app. the web app will continuesly check for any nearby messages by sending location to the server. if the user is nearby to allowed message or public message, the system will send a notification to user. the app should be fast , scalable and easy to use with good attractive UI. a sample UI is attached for reference in the design folder.
@design
